[project]
name = "deviza-vektor-matrix"
version = "0.1.0"
description = "Fundamental-market divergence and stopping-time reference engine for FX/XAU routing and risk controls."
requires-python = ">=3.11"
dependencies = ["numpy>=1.26"]

[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
